<mvc:View
    controllerName="zindirect_costs.controller.DetailsControllers.Diferidos"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    displayBlock="true"
    height="100%"
    xmlns="sap.m"
    xmlns:t="sap.ui.table"
    xmlns:uxap="sap.uxap"
    xmlns:vn="zindirect_costs.control"
>
    <uxap:ObjectPageLayout
        id="objectPageDiferidos"
        showHeaderContent="true"
        toggleHeaderOnTitleClick="true"
        alwaysShowContentHeader="false"
        headerContentPinnable="true"
        preserveHeaderStateOnScroll="true"
        height="100%"
    >
        <uxap:headerTitle>
            <uxap:ObjectPageDynamicHeaderTitle>
                <uxap:expandedHeading><Text text="" /></uxap:expandedHeading>
                <uxap:snappedHeading><Text text="" /></uxap:snappedHeading>
            </uxap:ObjectPageDynamicHeaderTitle>
        </uxap:headerTitle>
        <uxap:headerContent>
            <VBox width="100%" id="tablesContainer">
                <HBox width="100%" alignItems="Stretch" justifyContent="SpaceBetween">
                    <VBox width="49%" fitContainer="true">
                        <t:Table class="sapUiSizeCompact smallHeaderTable headerTableFix" rows="{dataTitle>/data}" visibleRowCount="1" width="100%" selectionMode="None">
                            <t:columns>
                                <t:Column hAlign="Center" class="operacionColumn">
                                    <VBox width="100%" class="fullWidthHeader" alignContent="Center">
                                        <Label text="{i18n>Aplicacion} {i18n>ejecutado}" design="Bold" />
                                    </VBox>
                                    <t:template>
                                        <Text textAlign="Center" width="100%" text="{parts: ['dataTitle>title','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}" />
                                    </t:template>
                                </t:Column>
                                <t:Column hAlign="Center">
                                    <VBox width="100%" class="fullWidthHeader">
                                        <Label text="{i18n>Aplicacion} {i18n>pendiente}" design="Bold" />
                                    </VBox>
                                    <t:template>
                                        <Text textAlign="Center" width="100%" text="{parts: ['dataTitle>title2','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}" />
                                    </t:template>
                                </t:Column>
                                <t:Column hAlign="Center">
                                    <VBox width="100%" class="fullWidthHeader">
                                        <Label text="{i18n>Aplicacion} {i18n>total}" design="Bold" />
                                    </VBox>
                                    <t:template>
                                        <Text textAlign="Center" width="100%" text="{parts: ['dataTitle>title3','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}" />
                                    </t:template>
                                </t:Column>
                            </t:columns>
                        </t:Table>
                    </VBox>
                    <VBox width="49%" fitContainer="true">
                        <t:Table class="sapUiSizeCompact smallHeaderTable headerTableFix" rows="{dataTitle>/data}" visibleRowCount="1" width="100%" selectionMode="None">
                            <t:columns>
                                <t:Column hAlign="Center">
                                    <Label text="{i18n>provision} {i18n>ejecutado}" design="Bold" />
                                    <t:template>
                                        <Text textAlign="Center" width="100%" text="{parts: ['dataTitle>title4','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}" />
                                    </t:template>
                                </t:Column>
                                <t:Column hAlign="Center">
                                    <Label text="{i18n>provision} {i18n>pendiente}" design="Bold" />
                                    <t:template>
                                        <Text textAlign="Center" width="100%" text="{parts: ['dataTitle>title5','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}" />
                                    </t:template>
                                </t:Column>
                                <t:Column hAlign="Center">
                                    <Label text="{i18n>provision} {i18n>total}" design="Bold" />
                                    <t:template>
                                        <Text textAlign="Center" width="100%" text="{parts: ['dataTitle>title6','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}" />
                                    </t:template>
                                </t:Column>
                            </t:columns>
                        </t:Table>
                    </VBox>
                </HBox>
            </VBox>
        </uxap:headerContent>
        <uxap:sections>
            <uxap:ObjectPageSection titleUppercase="false" id="inversionAmortizacion">
                <uxap:subSections>
                    <uxap:ObjectPageSubSection id="inversionAmortizacionSS1" titleUppercase="false">
                        <uxap:blocks>
                            <VBox width="100%" height="100%" class="noMarginTop">
                                <t:TreeTable
                                    id="TreeTableDiferidos"
                                    rows="{path: 'diferidosModel>/', parameters: {arrayNames: ['children']}}"
                                    columnResize="onColumnResize"
                                    toggleOpenState="onToggleOpenState"
                                    rowSelectionChange="onRowSelectionChange"
                                    visibleRowCount="{viewModel>/dynamicRowCount}"
                                    fixedColumnCount="2"
                                    class="sapUiSizeCompact"
                                    width="100%"
                                >
                                    <t:extension>
                                        <OverflowToolbar height="100%">
                                            <SearchField width="200px" placeholder="{i18n>operacion}" class="allowLetters" enableSuggestions="true" suggest="onOperacionSuggest" liveChange="onOperacionLiveChange" search="onOperacionSearch" />
                                            <Button width="8%" text="{i18n>TBorigen}" press="onOriginPress" />
                                            <Label text="{i18n>reales}" class="sapUiSmallMarginBegin" />
                                            <Label text="{path: 'dashboardModel>/NavMasterLt/0/Freal', type: 'sap.ui.model.type.Date', formatOptions: {pattern: 'dd/MM/yyyy'}}" />
                                            <Label text="{i18n>OEO}" class="sapUiMediumMarginBegin" />
                                            <Label text="{parts: ['dashboardModel>/NavMasterLt/0/Oeo', 'dashboardModel>/decimales', 'appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}" class="sapUiMediumMarginEnd" />
                                            <CheckBox id="checkboxInversion" text="{i18n>Aplicacion}" selected="true" select="onFilterTipoIndChange" />
                                            <CheckBox id="checkboxAmortizacion" text="{i18n>provision}" selected="true" select="onFilterTipoIndChange" />
                                            <ToolbarSpacer />
                                            <CheckBox id="idEjecutadoCheckBox2" text="{i18n>cbEjecutado}" class="alignTextCheckBox" select="onEjecutadoCheckBoxSelect" />
                                            <CheckBox id="idAjustesCheckBox" text="{i18n>cbAjustes}" class="alignTextCheckBox" select="onAjustesCheckBox" />
                                            <Select items="{yearsModel>/years}" selectedKey="{yearsModel>/selectedYear}" forceSelection="true" width="100px" change="onYearChange" class="sapUiSizeCompact yearSelect" valueState="{yearsModel>/valueState}" valueStateText="{yearsModel>/valueStateText}">
                                                <core:Item key="{yearsModel>year}" text="{yearsModel>year}" />
                                            </Select>
                                            <Button icon="sap-icon://add" press="onAddPress" class="toolbarActionBtn" tooltip="{i18n>anadir}" enabled="{modeloBloqueo>/isBlocked}" />
                                            <Button icon="sap-icon://delete" press="onDeletePress" class="toolbarActionBtn" tooltip="{i18n>eliminar}" enabled="{modeloBloqueo>/isBlocked}" />
                                            <Button icon="sap-icon://shortcut" press="onShortcutPress" class="toolbarActionBtn" tooltip="{i18n>repartir}" />
                                            <Button icon="sap-icon://arrow-bottom" press="onProcessPress" class="toolbarActionBtn" tooltip="{i18n>subirDesglose}" />
                                            <ToolbarSeparator />
                                            <Button id="btnVariantSelector" text="{variantModel>/displayLabel}" iconFirst="false" type="Transparent" press="onOpenVariantPopover" />
                                        </OverflowToolbar>
                                    </t:extension>
                                    <t:columns>
                                        <!-- COLUMNA 1: Operación / PhPspnr -->
                                        <t:Column width="180px" hAlign="Center">
                                            <t:label>
                                                <VBox width="100%" class="fullWidthHeader">
                                                    <Label text="{i18n>operacion}" class="titleGrande" design="Bold" />
                                                    <HBox renderType="Bare" visible="{ui>/showStickyParent}" class="parentHeaderBox">
                                                        <Text text="{ui>/stickyHeaderData/parent/name}" wrapping="false" width="100%" textAlign="Center" />
                                                    </HBox>
                                                    <VBox>
                                                        <HBox class="parentHeader" renderType="Bare" alignContent="Start">
                                                            <Button visible="{ui>/showStickyChild}" class="childHeaderBtn sapUiTinyMarginEnd" icon="sap-icon://navigation-up-arrow" press="onCollapseFromHeader" />
                                                            <Text text="{ui>/stickyHeaderData/child/name}" width="100%" textAlign="Center" class="secondStickyText" wrapping="false" visible="{ui>/showStickyChild}" />
                                                        </HBox>
                                                    </VBox>
                                                </VBox>
                                            </t:label>
                                            <t:template>
                                                <VBox renderType="Bare" width="100%" alignContent="Center">
                                                    <!-- Fila cabecera de desglose: muestra etiqueta "Agrupador" + botón de agrupación -->
                                                    <HBox renderType="Bare" width="100%" alignItems="Center" justifyContent="Center"
                                                        visible="{= ${diferidosModel>__isHeader} === true }">
                                                        <Input value="{diferidosModel>_headerAgrup}" editable="false" class="flatCellInput" width="100%" textAlign="Center" />
                                                        <Button
                                                            icon="sap-icon://paging"
                                                            type="{= ${diferidosModel>__agrupadorActive} === true ? 'Emphasized' : 'Transparent' }"
                                                            press="onAgrupadorButtonPress"
                                                            class="sapUiTinyMarginBegin"
                                                            tooltip="{i18n>tooltipAgrupar}"
                                                        />
                                                    </HBox>
                                                     <!-- Fila normal (operación / capítulo): muestra PhPspnr + botón añadir desglose para Inversión -->
                                                     <HBox renderType="Bare" alignItems="Center" width="100%"
                                                         visible="{= ${diferidosModel>__isEditable} !== true &amp;&amp; ${diferidosModel>__isSinProveedor} !== true &amp;&amp; ${diferidosModel>__isHeader} !== true }">
                                                         <Input
                                                             width="100%"
                                                             textAlign="Center"
                                                             editable="{= ${diferidosModel>isLevel3} &amp;&amp; ${diferidosModel>isNew} }"
                                                             enabled="{= ${diferidosModel>__isAgrupadorTotal} !== true }"
                                                             value="{diferidosModel>PhPspnr}"
                                                             change="onPhPspnrChange"
                                                             class="flatCellInput allowLetters"
                                                         />
                                                         <Button
                                                             icon="sap-icon://add"
                                                             width="1.5rem"
                                                             type="Transparent"
                                                             press="onAddDesglosePress"
                                                             visible="{= ${diferidosModel>TipoInd} === 'P' &amp;&amp; ${diferidosModel>isNew} !== true &amp;&amp; ${diferidosModel>PhPspnr} !== 'D' &amp;&amp; ${modeloBloqueo>/isBlocked} === true }"
                                                             enabled="{modeloBloqueo>/isBlocked}"
                                                             tooltip="{i18n>anadir}"
                                                             class="sapUiTinyMarginEnd"
                                                         />
                                                    </HBox>
                                                    <!-- Fila editable de desglose (con proveedor): muestra AGRUP editable -->
                                                    <Input
                                                        value="{diferidosModel>AGRUP}"
                                                        class="sapUiSizeCompact allowLetters"
                                                        width="100%"
                                                        textAlign="Center"
                                                        change="onDesglosAGRUPChange"
                                                        visible="{= ${diferidosModel>__isEditable} === true }"
                                                    />
                                                    <!-- Fila "Resto de proveedores" (solo lectura) -->
                                                    <Input
                                                        value="{diferidosModel>AGRUP}"
                                                        editable="false"
                                                        class="flatCellInput placeholderLike"
                                                        width="100%"
                                                        textAlign="Center"
                                                        visible="{= ${diferidosModel>__isSinProveedor} === true }"
                                                    />
                                                </VBox>
                                            </t:template>
                                        </t:Column>
                                        <!-- COLUMNA 2: Descripción / Post1 / DESCRIP -->
                                        <t:Column width="160px" hAlign="Center">
                                            <t:label>
                                                <VBox width="100%" class="fullWidthHeader">
                                                    <Label text="{i18n>descripcionOperacion}" class="titleGrande" design="Bold" />
                                                    <VBox renderType="Bare" visible="{ui>/showStickyParent}" class="parentHeaderBox">
                                                        <Text text="{ui>/stickyHeaderData/parent/currency}" wrapping="false" width="100%" textAlign="Center" />
                                                    </VBox>
                                                    <VBox class="parentHeader">
                                                        <Text text="{ui>/stickyHeaderData/child/currency}" textAlign="Center" wrapping="false" class="secondStickyText" visible="{ui>/showStickyChild}" />
                                                    </VBox>
                                                </VBox>
                                            </t:label>
                                            <t:template>
                                                <VBox renderType="Bare" width="100%">
                                                    <!-- Fila cabecera de desglose: muestra etiqueta "Descripción" -->
                                                    <Input value="{diferidosModel>_headerDescr}" editable="false" width="100%" textAlign="Center" class="flatCellInput"
                                                        visible="{= ${diferidosModel>__isHeader} === true }" />
                                                     <!-- Fila normal: muestra Post1 editable según contexto -->
                                                     <Input
                                                         value="{diferidosModel>Post1}"
                                                         width="9rem"
                                                         type="Text"
                                                         editable="{= ${diferidosModel>isLevel3} &amp;&amp; ${diferidosModel>isNew} }"
                                                         enabled="{= ${diferidosModel>__isAgrupadorTotal} !== true }"
                                                         textAlign="Center"
                                                         change="onDescOperacionChange"
                                                         class="flatCellInput allowLetters"
                                                         visible="{= ${diferidosModel>__isEditable} !== true &amp;&amp; ${diferidosModel>__isSinProveedor} !== true &amp;&amp; ${diferidosModel>__isHeader} !== true }"
                                                     />
                                                    <!-- Fila editable de desglose: muestra DESCRIP editable -->
                                                    <Input
                                                        value="{diferidosModel>DESCRIP}"
                                                        class="sapUiSizeCompact allowLetters"
                                                        width="100%"
                                                        textAlign="Center"
                                                        visible="{= ${diferidosModel>__isEditable} === true }"
                                                    />
                                                    <!-- Fila "Resto de proveedores": muestra Post1 solo lectura -->
                                                    <Input
                                                        value="{diferidosModel>Post1}"
                                                        editable="false"
                                                        class="flatCellInput placeholderLike"
                                                        width="100%"
                                                        textAlign="Center"
                                                        visible="{= ${diferidosModel>__isSinProveedor} === true }"
                                                    />
                                                </VBox>
                                            </t:template>
                                        </t:Column>
                                        <!-- COLUMNA 3: Tipo (Inv/Amo) / Proveedor LIFNR en desgloses -->
                                        <t:Column width="120px" hAlign="Center">
                                            <t:label>
                                                <VBox width="100%" class="fullWidthHeader">
                                                    <Label text="{i18n>Tipo}" class="titleGrande" design="Bold" />
                                                    <VBox renderType="Bare" visible="{ui>/showStickyParent}" class="parentHeaderBox">
                                                        <Text text="{ui>/stickyHeaderData/parent/amount}" width="100%" textAlign="Center" wrapping="false" />
                                                    </VBox>
                                                    <VBox class="parentHeader">
                                                        <Text text="{ui>/stickyHeaderData/child/amount}" textAlign="Center" wrapping="false" class="secondStickyText" visible="{ui>/showStickyChild}" />
                                                    </VBox>
                                                </VBox>
                                            </t:label>
                                            <t:template>
                                                <VBox renderType="Bare" width="100%" alignContent="Center">
                                                     <!-- Fila normal: muestra TipoInd formateado -->
                                                     <Input
                                                         width="100%"
                                                         textAlign="Center"
                                                         editable="false"
                                                         enabled="{= ${diferidosModel>__isAgrupadorTotal} !== true }"
                                                         value="{path: 'diferidosModel>TipoInd', formatter: '.formatTipoInd'}"
                                                         class="flatCellInput allowLetters"
                                                         visible="{= ${diferidosModel>__isEditable} !== true &amp;&amp; ${diferidosModel>__isSinProveedor} !== true &amp;&amp; ${diferidosModel>__isHeader} !== true }"
                                                     />
                                                    <!-- Fila cabecera de desglose: muestra etiqueta "Proveedor" -->
                                                    <Input
                                                        value="{diferidosModel>_headerProv}"
                                                        editable="false"
                                                        class="flatCellInput"
                                                        width="100%"
                                                        textAlign="Center"
                                                        visible="{= ${diferidosModel>__isHeader} === true }"
                                                    />
                                                    <!-- Fila editable desglose: LIFNR editable con ayuda de búsqueda -->
                                                    <Input
                                                        value="{diferidosModel>LIFNR}"
                                                        width="100%"
                                                        textAlign="Center"
                                                        class="flatCellInput allowLetters proveedorVHInput"
                                                        change="onDesgloseProveedorChange"
                                                        editable="{= ${modeloBloqueo>/isBlocked} === true }"
                                                        showValueHelp="{= ${modeloBloqueo>/isBlocked} === true }"
                                                        valueHelpRequest="onProveedorValueHelpRequest"
                                                        visible="{= ${diferidosModel>__isEditable} === true }"
                                                    />
                                                    <!-- Fila "Resto de proveedores": vacío -->
                                                    <Input value="" editable="false" width="100%" class="flatCellInput placeholderLike"
                                                        visible="{= ${diferidosModel>__isSinProveedor} === true }" />
                                                </VBox>
                                            </t:template>
                                        </t:Column>
                                        <!-- COLUMNA 4: Inv Eje (oculta por defecto) -->
                                        <t:Column width="80px" hAlign="Center" visible="false">
                                            <t:label>
                                                <VBox width="100%" class="fullWidthHeader">
                                                    <Label text="{i18n>inveje}" class="titleGrande" design="Bold" />
                                                    <VBox renderType="Bare" visible="{ui>/showStickyParent}" class="parentHeaderBox">
                                                        <Text text="{ui>/stickyHeaderData/parent/amount}" width="100%" textAlign="Center" wrapping="false" />
                                                    </VBox>
                                                    <VBox class="parentHeader">
                                                        <Text text="{ui>/stickyHeaderData/child/amount}" textAlign="Center" wrapping="false" class="secondStickyText" visible="{ui>/showStickyChild}" />
                                                    </VBox>
                                                </VBox>
                                            </t:label>
                                            <t:template>
                                                <HBox renderType="Bare" width="100%" alignItems="Center">
                                                    <Button icon="sap-icon://filter" press="filterTableRow" class="bottomFilter" width="0.9rem" visible="{= ${cabecera} === true}">
                                                        <customData><core:CustomData key="filterProp" value="InvEje" /></customData>
                                                    </Button>
                                                    <vn:DecimalesInput width="4.2rem" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center" editable="false"
                                                        value="{parts: ['diferidosModel>InvEje','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                        class="flatCellInput" />
                                                </HBox>
                                            </t:template>
                                        </t:Column>
                                        <!-- COLUMNA 5: Inv Eje Real (oculta por defecto) -->
                                        <t:Column width="80px" hAlign="Center" visible="false">
                                            <t:label>
                                                <VBox width="100%" class="fullWidthHeader">
                                                    <Label text="{i18n>Invejereal}" class="titleGrande" design="Bold" />
                                                    <VBox renderType="Bare" visible="{ui>/showStickyParent}" class="parentHeaderBox">
                                                        <Text text="{ui>/stickyHeaderData/parent/amount}" width="100%" textAlign="Center" wrapping="false" />
                                                    </VBox>
                                                    <VBox class="parentHeader">
                                                        <Text text="{ui>/stickyHeaderData/child/amount}" textAlign="Center" wrapping="false" class="secondStickyText" visible="{ui>/showStickyChild}" />
                                                    </VBox>
                                                </VBox>
                                            </t:label>
                                            <t:template>
                                                <HBox renderType="Bare" width="100%" alignItems="Center">
                                                    <Button icon="sap-icon://filter" press="filterTableRow" class="bottomFilter" width="0.9rem" visible="{= ${cabecera} === true}">
                                                        <customData><core:CustomData key="filterProp" value="Invejereal" /></customData>
                                                    </Button>
                                                    <vn:DecimalesInput width="4.2rem" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center" editable="false"
                                                        value="{parts: ['diferidosModel>Invejereal','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                        class="flatCellInput" />
                                                </HBox>
                                            </t:template>
                                        </t:Column>
                                        <!-- COLUMNA 6: Inv Eje Ajus (oculta por defecto) -->
                                        <t:Column width="80px" hAlign="Center" visible="false">
                                            <t:label>
                                                <VBox width="100%" class="fullWidthHeader">
                                                    <Label text="{i18n>Invejeajus}" class="titleGrande" design="Bold" />
                                                    <VBox renderType="Bare" visible="{ui>/showStickyParent}" class="parentHeaderBox">
                                                        <Text text="{ui>/stickyHeaderData/parent/amount}" width="100%" textAlign="Center" wrapping="false" />
                                                    </VBox>
                                                    <VBox class="parentHeader">
                                                        <Text text="{ui>/stickyHeaderData/child/amount}" textAlign="Center" wrapping="false" class="secondStickyText" visible="{ui>/showStickyChild}" />
                                                    </VBox>
                                                </VBox>
                                            </t:label>
                                            <t:template>
                                                <HBox renderType="Bare" width="100%" alignItems="Center">
                                                    <Button icon="sap-icon://filter" press="filterTableRow" class="bottomFilter" width="0.9rem" visible="{= ${cabecera} === true}">
                                                        <customData><core:CustomData key="filterProp" value="Invejeajus" /></customData>
                                                    </Button>
                                                    <vn:DecimalesInput width="4.2rem" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center" editable="false"
                                                        value="{parts: ['diferidosModel>Invejeajus','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                        class="flatCellInput" />
                                                </HBox>
                                            </t:template>
                                        </t:Column>
                                        <!-- COLUMNA 7: Coste Ejecutado (_Ejecutado / INVEJE) -->
                                        <t:Column width="80px" hAlign="Center">
                                            <t:label>
                                                <VBox width="100%" class="fullWidthHeader">
                                                    <Label text="{i18n>coste}" class="titleGrande" design="Bold" />
                                                    <VBox renderType="Bare" visible="{ui>/showStickyParent}" class="parentHeaderBox">
                                                        <Text text="{ui>/stickyHeaderData/parent/amount}" width="100%" textAlign="Center" wrapping="false" />
                                                    </VBox>
                                                    <VBox class="parentHeader">
                                                        <Text text="{ui>/stickyHeaderData/child/amount}" textAlign="Center" wrapping="false" class="secondStickyText" visible="{ui>/showStickyChild}" />
                                                    </VBox>
                                                </VBox>
                                            </t:label>
                                            <t:template>
                                                <VBox renderType="Bare" width="100%">
                                                    <!-- Fila cabecera de desglose: muestra etiqueta "Ejecutado" -->
                                                    <Input value="{diferidosModel>_headerEje}" editable="false" width="100%" textAlign="Center" class="flatCellInput"
                                                        visible="{= ${diferidosModel>__isHeader} === true }" />
                                                    <!-- Fila normal: _Ejecutado no editable -->
                                                    <HBox renderType="Bare" width="100%" alignItems="Center"
                                                        visible="{= ${diferidosModel>__isEditable} !== true &amp;&amp; ${diferidosModel>__isSinProveedor} !== true &amp;&amp; ${diferidosModel>__isHeader} !== true }">
                                                        <Button icon="sap-icon://filter" press="filterTableRow" class="bottomFilter" width="0.9rem" visible="{= ${cabecera} === true}">
                                                            <customData><core:CustomData key="filterProp" value="_Ejecutado" /></customData>
                                                        </Button>
                                                        <vn:DecimalesInput width="4.2rem" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center" editable="false"
                                                            value="{parts: ['diferidosModel>_Ejecutado','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                            class="flatCellInput" />
                                                    </HBox>
                                                    <!-- Fila editable desglose: INVEJE no editable -->
                                                    <vn:DecimalesInput width="100%" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center" editable="false"
                                                        value="{parts: ['diferidosModel>INVEJE','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                        class="flatCellInput"
                                                        visible="{= ${diferidosModel>__isEditable} === true }" />
                                                    <!-- Fila "Resto de proveedores": INVEJE no editable -->
                                                    <vn:DecimalesInput width="100%" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center" editable="false"
                                                        value="{parts: ['diferidosModel>INVEJE','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                        class="flatCellInput"
                                                        visible="{= ${diferidosModel>__isSinProveedor} === true }" />
                                                </VBox>
                                            </t:template>
                                        </t:Column>
                                        <!-- COLUMNA 8: Coste Pendiente (_Pendiente / INVPEN) -->
                                        <t:Column width="80px" hAlign="Center">
                                            <t:label>
                                                <VBox width="100%" class="fullWidthHeader">
                                                    <Label text="{i18n>costepend}" design="Bold" class="titleGrande" />
                                                    <VBox renderType="Bare" visible="{ui>/showStickyParent}" class="parentHeaderBox">
                                                        <Text text="{ui>/stickyHeaderData/parent/pricepending}" width="100%" textAlign="Center" wrapping="false" />
                                                    </VBox>
                                                    <VBox class="parentHeader">
                                                        <Text text="{ui>/stickyHeaderData/child/pricepending}" textAlign="Center" wrapping="false" class="secondStickyText" visible="{ui>/showStickyChild}" />
                                                    </VBox>
                                                </VBox>
                                            </t:label>
                                            <t:template>
                                                <VBox renderType="Bare" width="100%">
                                                    <!-- Fila cabecera de desglose: muestra etiqueta "Pendiente" -->
                                                    <Input value="{diferidosModel>_headerPend}" editable="false" width="100%" textAlign="Center" class="flatCellInput"
                                                        visible="{= ${diferidosModel>__isHeader} === true }" />
                                                    <!-- Fila normal: _Pendiente editable para Inv -->
                                                    <HBox renderType="Bare" width="100%" alignItems="Center"
                                                        visible="{= ${diferidosModel>__isEditable} !== true &amp;&amp; ${diferidosModel>__isSinProveedor} !== true &amp;&amp; ${diferidosModel>__isHeader} !== true }">
                                                        <Button icon="sap-icon://filter" press="filterTableRow" width="0.9rem" class="bottomFilter" visible="{= ${cabecera} === true}">
                                                            <customData><core:CustomData key="filterProp" value="_Pendiente" /></customData>
                                                        </Button>
                                                        <vn:DecimalesInput width="4.2rem" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center"
                                                            editable="{= ${diferidosModel>Estructura} === 'O' &amp;&amp; ${diferidosModel>TipoInd} === 'P' &amp;&amp; ${diferidosModel>isNew} === false &amp;&amp; ${modeloBloqueo>/isBlocked} === true }"
                                                            enabled="{= ${diferidosModel>__isAgrupadorTotal} !== true }"
                                                            change="onAmoPenChange"
                                                            value="{parts: ['diferidosModel>_Pendiente','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                            class="flatCellInput" />
                                                    </HBox>
                                                    <!-- Fila editable desglose: INVPEN editable -->
                                                    <vn:DecimalesInput width="100%" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center"
                                                        editable="{= ${modeloBloqueo>/isBlocked} === true }"
                                                        change="onDesgloseInvPenChange"
                                                        value="{parts: ['diferidosModel>INVPEN','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                        class="flatCellInput"
                                                        visible="{= ${diferidosModel>__isEditable} === true }" />
                                                    <!-- Fila "Resto de proveedores": INVPEN solo lectura -->
                                                    <vn:DecimalesInput width="100%" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center" editable="false"
                                                        value="{parts: ['diferidosModel>INVPEN','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                        class="flatCellInput"
                                                        visible="{= ${diferidosModel>__isSinProveedor} === true }" />
                                                </VBox>
                                            </t:template>
                                        </t:Column>
                                        <!-- COLUMNA 9: %Pendiente (_PctjPen: PctjPenInv para P / PctjPenAmo para B) -->
                                         <t:Column width="80px" hAlign="Center">
                                             <t:label>
                                                 <VBox width="100%" class="fullWidthHeader">
                                                     <Label text="{i18n>porcpend}" design="Bold" class="titleGrande" />
                                                     <VBox renderType="Bare" visible="{ui>/showStickyParent}" class="parentHeaderBox">
                                                         <Text text="{ui>/stickyHeaderData/parent/pricepending}" width="100%" textAlign="Center" wrapping="false" />
                                                     </VBox>
                                                     <VBox class="parentHeader">
                                                         <Text text="{ui>/stickyHeaderData/child/pricepending}" textAlign="Center" wrapping="false" class="secondStickyText" visible="{ui>/showStickyChild}" />
                                                     </VBox>
                                                 </VBox>
                                             </t:label>
                                             <t:template>
                                                 <VBox renderType="Bare" width="100%">
                                                     <!-- Fila cabecera de desglose -->
                                                     <Input value="{diferidosModel>_headerPctj}" editable="false" width="100%" textAlign="Center" class="flatCellInput" visible="{= ${diferidosModel>__isHeader} === true }" />
                                                     <!-- Fila normal: PctjPenAmo editable para P -->
                                                     <HBox renderType="Bare" width="100%" alignItems="Center" visible="{= ${diferidosModel>__isEditable} !== true &amp;&amp; ${diferidosModel>__isSinProveedor} !== true &amp;&amp; ${diferidosModel>__isHeader} !== true }">
                                                         <Button icon="sap-icon://filter" press="filterTableRow" width="0.9rem" class="bottomFilter" visible="{= ${cabecera} === true}">
                                                             <customData><core:CustomData key="filterProp" value="_PctjPen" /></customData>
                                                         </Button>
                                                         <!--     Se sustituye sap.m.Input por vn:DecimalesInput
                                                              con value via parts/formatter (.formatDecimales) replicando
                                                              el patron usado por las celdas numericas que ya funcionan
                                                              (AmoPen, AmoTot, etc.). Sin formatter de display el valor
                                                              que el handler normaliza a formato SAP (p.ej. "2.00000") y
                                                              el que devuelve _mergeBackendRowsIntoTree tras la respuesta
                                                              del backend se mostraban tal cual, pisando lo que el usuario
                                                              acababa de teclear. Con formatter el modelo guarda el raw y
                                                              la UI muestra el numero ya formateado segun los decimales y
                                                              separadores del usuario. -->
                                                         <vn:DecimalesInput width="4.2rem" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center" editable="{= ${diferidosModel>Estructura} === 'O' &amp;&amp; ${diferidosModel>TipoInd} === 'P' &amp;&amp; ${diferidosModel>isNew} === false &amp;&amp; ${modeloBloqueo>/isBlocked} === true }" change="onPercPenAmo" value="{parts: ['diferidosModel>_PctjPen','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}" class="flatCellInput" />
                                                         <!--    -->
                                                     </HBox>
                                                     <!-- Fila editable desglose: PCTJ -->
                                                     <Input value="{diferidosModel>PCTJ}" class="sapUiSizeCompact" width="100%" textAlign="Center" visible="{= ${diferidosModel>__isEditable} === true }" />
                                                     <!-- Fila resto de proveedores -->
                                                     <Input value="" editable="false" width="100%" class="flatCellInput placeholderLike" visible="{= ${diferidosModel>__isSinProveedor} === true }" />
                                                 </VBox>
                                             </t:template>
                                         </t:Column>
                                         <!-- COLUMNA 10: Coste Total (_Total / INVTOT) -->
                                        <t:Column width="80px" hAlign="Center">
                                            <t:label>
                                                <VBox width="100%" class="fullWidthHeader">
                                                    <Label text="{i18n>costetotal}" class="titleGrande" design="Bold" />
                                                    <VBox renderType="Bare" visible="{ui>/showStickyParent}" class="parentHeaderBox">
                                                        <Text text="{ui>/stickyHeaderData/parent/pricetotal}" width="100%" textAlign="Center" wrapping="false" />
                                                    </VBox>
                                                    <VBox class="parentHeader">
                                                        <Text text="{ui>/stickyHeaderData/child/pricetotal}" textAlign="Center" wrapping="false" class="secondStickyText" visible="{ui>/showStickyChild}" />
                                                    </VBox>
                                                </VBox>
                                            </t:label>
                                            <t:template>
                                                <VBox renderType="Bare" width="100%">
                                                    <!-- Fila cabecera de desglose: muestra etiqueta "Total" -->
                                                    <Input value="{diferidosModel>_headerTot}" editable="false" width="100%" textAlign="Center" class="flatCellInput"
                                                        visible="{= ${diferidosModel>__isHeader} === true }" />
                                                    <!-- Fila normal: _Total editable para Inv -->
                                                    <HBox renderType="Bare" width="100%" alignItems="Center"
                                                        visible="{= ${diferidosModel>__isEditable} !== true &amp;&amp; ${diferidosModel>__isSinProveedor} !== true &amp;&amp; ${diferidosModel>__isHeader} !== true }">
                                                        <Button icon="sap-icon://filter" press="filterTableRow" width="0.9rem" class="bottomFilter" visible="{= ${cabecera} === true}">
                                                            <customData><core:CustomData key="filterProp" value="_Total" /></customData>
                                                        </Button>
                                                        <vn:DecimalesInput width="4.2rem" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center"
                                                            editable="{= ${diferidosModel>Estructura} === 'O' &amp;&amp; ${diferidosModel>TipoInd} === 'P' &amp;&amp; ${diferidosModel>isNew} === false &amp;&amp; ${modeloBloqueo>/isBlocked} === true }"
                                                            enabled="{= ${diferidosModel>__isAgrupadorTotal} !== true }"
                                                            change="onAmoTotChange"
                                                            value="{parts: ['diferidosModel>_Total','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                            class="flatCellInput" />
                                                    </HBox>
                                                    <!-- Fila editable desglose: INVTOT editable -->
                                                    <vn:DecimalesInput width="100%" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center"
                                                        editable="{= ${modeloBloqueo>/isBlocked} === true }"
                                                        change="onDesgloseInvTotChange"
                                                        value="{parts: ['diferidosModel>INVTOT','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                        class="flatCellInput"
                                                        visible="{= ${diferidosModel>__isEditable} === true }" />
                                                    <!-- Fila "Resto de proveedores": INVTOT solo lectura -->
                                                    <vn:DecimalesInput width="100%" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center" editable="false"
                                                        value="{parts: ['diferidosModel>INVTOT','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                        class="flatCellInput"
                                                        visible="{= ${diferidosModel>__isSinProveedor} === true }" />
                                                </VBox>
                                            </t:template>
                                        </t:Column>
                                        <!-- COLUMNA 11: Reparto / Tipo -->
                                        <t:Column id="colStart" width="115px" hAlign="Center">
                                            <t:label>
                                                <VBox width="100%" class="fullWidthHeader">
                                                    <Label text="{i18n>dbReparto}" design="Bold" class="titleGrande" textAlign="Center" />
                                                    <VBox renderType="Bare" visible="{ui>/showStickyParent}" class="parentHeaderBox">
                                                        <Text text="{ui>/stickyHeaderData/parent/size}" width="100%" textAlign="Center" wrapping="false" />
                                                    </VBox>
                                                    <VBox class="parentHeader">
                                                        <Text text="{ui>/stickyHeaderData/child/size}" textAlign="Center" wrapping="false" class="secondStickyText" visible="{ui>/showStickyChild}" />
                                                    </VBox>
                                                </VBox>
                                            </t:label>
                                            <t:template>
                                                <VBox renderType="Bare" width="100%">
                                                    <!-- Fila cabecera de desglose: muestra etiqueta "Reparto" -->
                                                    <Input value="{diferidosModel>_headerReparto}" editable="false" width="100%" textAlign="Center" class="flatCellInput"
                                                        visible="{= ${diferidosModel>__isHeader} === true }" />
                                                    <!-- Fila normal: Select de reparto + calendario (LIN) -->
                                                    <HBox width="100%" alignContent="Center" alignItems="Center" justifyContent="Center" renderType="Bare"
                                                        visible="{= ${diferidosModel>__isEditable} !== true &amp;&amp; ${diferidosModel>__isSinProveedor} !== true &amp;&amp; ${diferidosModel>__isHeader} !== true }">
                                                        <Button icon="sap-icon://filter" press="filterTableRow" class="bottomFilter" width="0.9rem" visible="{= ${cabecera} === true}">
                                                            <customData><core:CustomData key="filterProp" value="Tipo" /></customData>
                                                        </Button>
                                                        <Select
                                                            selectedKey="{diferidosModel>Tipo}"
                                                            width="auto"
                                                            class="flatCellInput"
                                                            change="onRepartoChange"
                                                            editable="{= ${diferidosModel>isNew} === false &amp;&amp; ${modeloBloqueo>/isBlocked} === true }"
                                                            enabled="{= ${diferidosModel>__isAgrupadorTotal} !== true }"
                                                            items="{path: 'diferidosModel>repartoItems', templateShareable: false}"
                                                        >
                                                            <core:Item key="{diferidosModel>key}" text="{path: 'diferidosModel>key', formatter: '.formatRepartoTipo'}" />
                                                        </Select>
                                                        <Button
                                                            icon="sap-icon://calendar"
                                                            visible="{= ${diferidosModel>Tipo} === 'LIN' }"
                                                            enabled="{= ${diferidosModel>PhPspnr} !== 'D' }"
                                                            type="Transparent"
                                                            press="onOpenRangePicker"
                                                            width="auto"
                                                            tooltip="{
                                                                parts: [{
                                                                    path: 'diferidosModel>_linDateFrom'
                                                                }, {
                                                                    path: 'diferidosModel>_linDateTo'
                                                                }],
                                                                formatter: '.getCalendarTooltip'
                                                            }"
                                                        />
                                                    </HBox>
                                                    <!-- Fila desglose editable: selector con opción única Manual (siempre MAN en desgloses de inversión) -->
                                                    <Select
                                                        selectedKey="{diferidosModel>TIPO}"
                                                        width="100%"
                                                        class="flatCellInput"
                                                        enabled="{= ${modeloBloqueo>/isBlocked} === true }"
                                                        visible="{= ${diferidosModel>__isEditable} === true }"
                                                    >
                                                        <core:Item key="MAN" text="{i18n>tipoManual}" />
                                                    </Select>
                                                    <!-- Fila "Resto de proveedores": vacío -->
                                                    <Input value="" editable="false" width="100%" textAlign="Center" class="flatCellInput placeholderLike"
                                                        visible="{= ${diferidosModel>__isSinProveedor} === true }" />
                                                </VBox>
                                            </t:template>
                                        </t:Column>
                                        <!-- COLUMNA 12: Pendiente a planificar (PenPlan / PENPLAN) -->
                                        <t:Column id="colEnd" width="100px" hAlign="Center">
                                            <t:label>
                                                <VBox width="100%" class="fullWidthHeader">
                                                    <Label text="{i18n>dbPendPlanificar}" design="Bold" class="titleGrande borderRightPend" textAlign="Center" />
                                                    <VBox renderType="Bare" visible="{ui>/showStickyParent}" class="parentHeaderBox">
                                                        <Text text="{ui>/stickyHeaderData/parent/last}" width="100%" textAlign="Center" wrapping="false" />
                                                    </VBox>
                                                    <VBox class="parentHeader">
                                                        <Text text="{ui>/stickyHeaderData/child/last}" textAlign="Center" wrapping="false" class="secondStickyText" visible="{ui>/showStickyChild}" />
                                                    </VBox>
                                                </VBox>
                                            </t:label>
                                            <t:template>
                                                <VBox renderType="Bare" width="100%" class="borderRightPend">
                                                    <!-- Fila cabecera de desglose: muestra etiqueta "Pend. a planificar" -->
                                                    <Input value="{diferidosModel>_headerPenPlan}" editable="false" width="100%" textAlign="Center" class="flatCellInput"
                                                        visible="{= ${diferidosModel>__isHeader} === true }" />
                                                    <!-- Fila normal: PenPlan no editable -->
                                                    <HBox width="100%" alignContent="Center" renderType="Bare"
                                                        visible="{= ${diferidosModel>__isEditable} !== true &amp;&amp; ${diferidosModel>__isSinProveedor} !== true &amp;&amp; ${diferidosModel>__isHeader} !== true }">
                                                        <Button icon="sap-icon://filter" press="filterTableRow" class="bottomFilter" width="0.9rem" visible="{= ${cabecera} === true}">
                                                            <customData><core:CustomData key="filterProp" value="PenPlan" /></customData>
                                                        </Button>
                                                        <vn:DecimalesInput width="4.2rem" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center" editable="false"
                                                            value="{parts: ['diferidosModel>PenPlan','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                            class="{= ${diferidosModel>PenPlan} !== 0 ? 'flatCellInput penPlanNegative' : 'flatCellInput' }" />
                                                    </HBox>
                                                    <!-- Fila editable desglose: PENPLAN no editable, resaltado si != 0 -->
                                                    <vn:DecimalesInput width="100%" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center" editable="false"
                                                        value="{parts: ['diferidosModel>PENPLAN','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                        class="{= ${diferidosModel>PENPLAN} !== '0' &amp;&amp; ${diferidosModel>PENPLAN} !== '' ? 'flatCellInput penPlanHighlight' : 'flatCellInput' }"
                                                        visible="{= ${diferidosModel>__isEditable} === true }" />
                                                    <!-- Fila "Resto de proveedores": PENPLAN no editable -->
                                                    <vn:DecimalesInput width="100%" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center" editable="false"
                                                        value="{parts: ['diferidosModel>PENPLAN','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                        class="flatCellInput"
                                                        visible="{= ${diferidosModel>__isSinProveedor} === true }" />
                                                </VBox>
                                            </t:template>
                                        </t:Column>
                                        <!-- COLUMNA 13: months placeholder (columnas dinámicas de meses se añaden por código) -->
                                        <t:Column id="colMonths" width="60px" hAlign="Center" visible="false">
                                            <t:label>
                                                <VBox width="100%" class="fullWidthHeader">
                                                    <Label text="&#160;" />
                                                    <VBox renderType="Bare" visible="{ui>/showStickyParent}" class="parentHeaderBox">
                                                        <Text text="&#160;" />
                                                    </VBox>
                                                    <VBox class="parentHeader">
                                                        <Text text="{ui>/stickyHeaderData/child/months}" textAlign="Center" wrapping="false" class="secondStickyText" visible="{ui>/showStickyChild}" />
                                                    </VBox>
                                                </VBox>
                                            </t:label>
                                            <t:template>
                                                <HBox width="100%" alignContent="Center" renderType="Bare">
                                                    <Button icon="sap-icon://filter" press="filterTableRow" class="bottomFilter" width="0.9rem" visible="{= ${cabecera} === true}">
                                                        <customData><core:CustomData key="filterProp" value="months" /></customData>
                                                    </Button>
                                                    <Input value="{diferidosModel>months}"
                                                        editable="{= ${isGroup} !== true &amp;&amp; ${modeloBloqueo>/isBlocked} === true }"
                                                        class="flatCellInput"
                                                        width="{= ${cabecera} === true ? '4rem' : '85px'}"
                                                        textAlign="Center" />
                                                </HBox>
                                            </t:template>
                                        </t:Column>
                                        <!-- COLUMNA 14: pend placeholder -->
                                        <t:Column id="colNew" width="60px" hAlign="Center" visible="false">
                                            <t:label>
                                                <VBox width="100%" class="fullWidthHeader">
                                                    <Label text="&#160;" />
                                                    <VBox renderType="Bare" visible="{ui>/showStickyParent}" class="parentHeaderBox">
                                                        <Text text="&#160;" width="100%" textAlign="Center" />
                                                    </VBox>
                                                    <VBox class="parentHeader">
                                                        <Text text="{ui>/stickyHeaderData/child/pend}" visible="{ui>/showStickyChild}" width="100%"
                                                            textAlign="Center" wrapping="false" class="secondStickyText" />
                                                    </VBox>
                                                </VBox>
                                            </t:label>
                                            <t:template>
                                                <HBox width="100%" alignContent="Center" renderType="Bare">
                                                    <Button icon="sap-icon://filter" press="filterTableRow" class="bottomFilter" width="0.9rem" visible="{= ${cabecera} === true}">
                                                        <customData><core:CustomData key="filterProp" value="pend" /></customData>
                                                    </Button>
                                                    <vn:DecimalesInput width="4.2rem" decimalNumbers="{dashboardModel>/decimales}" textAlign="Center" editable="false"
                                                        value="{parts: ['diferidosModel>pend','dashboardModel>/decimales','appData>/userData/CurrencyFormat'], formatter: '.formatDecimales'}"
                                                        class="flatCellInput" />
                                                </HBox>
                                            </t:template>
                                        </t:Column>
                                    </t:columns>
                                </t:TreeTable>
                            </VBox>
                        </uxap:blocks>
                    </uxap:ObjectPageSubSection>
                </uxap:subSections>
            </uxap:ObjectPageSection>
        </uxap:sections>
    </uxap:ObjectPageLayout>
</mvc:View>
